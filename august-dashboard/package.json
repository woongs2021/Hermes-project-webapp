{
  "name": "august-dashboard",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run generate:data && vite",
    "generate:data": "npm run generate:manifest && npm run generate:home-visuals && npm run generate:research-board",
    "validate:data": "npm run validate:manifest && npm run validate:home-visuals && npm run validate:research-board",
    "generate:manifest": "node scripts/generate-dashboard-manifest.mjs",
    "validate:manifest": "node scripts/validate-dashboard-manifest.mjs",
    "generate:home-visuals": "node scripts/generate-home-visual-set.mjs",
    "validate:home-visuals": "node scripts/validate-home-visual-set.mjs",
    "generate:research-board": "node scripts/generate-research-board.mjs",
    "validate:research-board": "node scripts/validate-research-board.mjs",
    "build": "npm run validate:data && npm run generate:data && tsc -b && vite build",
    "lint": "oxlint",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.3",
    "oxlint": "^1.71.0",
    "typescript": "~6.0.2",
    "vite": "^8.1.1"
  }
}
