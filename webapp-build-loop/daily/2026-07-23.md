# 2026-07-23 KST — Faker 웹앱 구조 브리프

단계: 2026-07-31까지 7월 계획 전용. 앱 repo 변경 없음, 배포 가능한 코드 없음, build/deploy 없음.

## 사용자-facing DM 초안

Faker입니다.

🧱 오늘의 구조 후보 — `HomeVisualSetAdapter`를 홈의 첫 데이터 어댑터로 두는 안입니다. `/`의 `HomeVisualHero`는 `최종/current-home-visual-set.json`만 먼저 읽고, 1개 lead + 2개 supporting 카드로 고정합니다. 오늘 생성된 검토 대기 이미지는 최종 저장 전까지 홈 데이터로 승격하지 않습니다.

🔗 연결 데이터 — Go Youn-jung 2026-07-22 최종 3종: Lantern Guidance / Key Possibility / Anchor Stability, 그리고 `items.jsonl` 시각 아카이브 feed입니다. Son/Muyeol의 오늘 관점은 “작업 상태·판단 지점·아티팩트 관계 보존”입니다.

🧩 OBD/Hermes-growth 의미 — 홈이 단순 갤러리가 아니라 “오늘 Chris가 무엇을 판단했고, 어떤 시각 은유로 보존했는가”를 보여주는 시작 화면이 됩니다. 같은 이미지는 `/visuals`에서는 Pinterest식 archive card로 재사용됩니다.

⚠️ 8월 구현 전 확인할 것 — public home에는 polished title/metaphor/rationale만 보이고, source path·private prompt·운영 로그는 비공개/관리자 detail에만 둘지 확정이 필요합니다.

✅ 오늘 쌓을 결정 — 홈 첫 화면에서 최신 최종 3종을 항상 자동 교체할지, 아니면 Chris가 “이번 주 대표 visual set”으로 pin 할 수 있게 둘지 한 줄 의견만 남겨주세요.

## 구조 브리프

`HomeVisualSetAdapter`를 홈페이지 source gate와 freshness policy로 정의한다:

- primary read: `data/image-gallery/final/current-home-visual-set.json`
- canonical pointer가 없을 때만 fallback: 최신 finalized `data/image-gallery/YYYY-MM-DD.json` 또는 날짜별 `items.jsonl`
- `data/image-gallery/pending/`을 public/home UI로 절대 읽지 않는다
- placement를 `{ lead: VisualItem, supporting: VisualItem[] }`로 정규화한다
- static-card vs detail-video 동작을 보존한다
- `canonical_status`, `date_kst`, `prompt_visibility`, 선택적 `pinned_status`를 포함한다

## 데이터 후보

`HomeVisualSet` 필드:

- `set_id`
- `date_kst`
- `source_status`: `최종_현재 | finalized_archive | fallback_latest | missing`
- `is_pinned`
- `lead_item_id`
- `supporting_item_ids`
- `public_narrative`
- `private_metadata_available`
- `asset_health`: `all_stills_present | partial | missing`
- `detail_video_health`: `all_turntables_present | partial | missing`
- `archive_route`: `/visuals`

## 8월 구현 후보

repo/scope 승인 후 visual styling 전에 작은 adapter 1개 + 홈페이지 component contract를 먼저 구현한다:

1. canonical JSON parse
2. 정확히 3개 item과 필수 still path 검증
3. placement를 lead/supporting 카드로 매핑
4. `HomeVisualHero`에 안전한 공개 필드 노출
5. 승인된 경우에만 전체 private metadata를 비공개/관리자 detail drawer로 전달

## OBD/Hermes-growth 재사용 메모

홈페이지를 design-memory system으로 유지한다. 시각 은유는 Chris의 판단과 연결된 안정적인 ontology object가 되고, archive route는 더 넓은 시각 근거 기반으로 남는다.

## 리스크 / 의존성

pinning behavior, 공개/비공개 field boundary, 운영 파일 경로가 어떤 비공개 웹앱 view에라도 나타나도 되는지에 대한 Chris/Karina 승인이 필요하다. 현재 계획은 Muyeol guardrail을 적용한다: 원시 프롬프트/logs 없음, private IDs 없음, performance overclaims 없음.

## Chris 관찰 요청

홈이 항상 최신 최종 3-image set을 보여줘야 할까요, 아니면 Chris가 “현재 대표 세트”를 일주일 동안 pin하고 새 세트는 `/visuals`에 계속 쌓이게 할 수 있어야 할까요?
