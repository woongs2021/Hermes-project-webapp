# 2026-07-22 KST — Hermes/OBD 웹앱 코딩 구조 브리프

## 단계
- 7월 계획 전용: 2026-07-31 KST까지 프로덕션 코드, app repo 수정, 배포 가능한 앱 생성, build/deploy 실행 없음.

## 오늘의 웹앱 구조 브리프
- 구조 아이디어: `PublicSafeDecisionLog`를 `/growth` 안의 핵심 카드/타임라인 데이터로 둔다.
- 오늘 Son의 SNS 루프가 제안한 `상황은 추상화 → 어떤 역할의 도움 → 선택지 축소 → Chris의 최종 기준 → 다음 관찰` 5문장 구조를, SNS 초안뿐 아니라 미래 Hermes/OBD 대시보드의 공개/비공개 분리 데이터로 정규화한다.
- 제안 화면: `/growth/decision-log` 또는 `/growth`의 bento 섹션에서 public card는 추상화된 판단 구조만 보여주고, private drawer는 source lane, 관여 agent, 근거 refs, follow-up 지표 placeholder를 보관한다.

## 데이터 구조 후보
- `PublicSafeDecisionLog`
  - `date_kst`: 기록 날짜
  - `abstract_situation`: 실제 프로젝트명/이해관계자를 제거한 상황 요약
  - `agent_role_involved`: 공개 가능한 역할명 또는 role family
  - `options_narrowed`: 어떤 선택지가 줄었는지
  - `chris_최종_criterion`: Chris가 최종 책임진 판단 기준
  - `next_observation`: 이후 볼 신호/후속 질문
  - `public_summary`: SNS/홈페이지에 노출 가능한 3–5문장 요약
  - `private_context_note`: 내부 운영 맥락, 기본 비공개
  - `privacy_level`: public_summary | private_detail | internal_only
  - `source_refs`: 비밀값 없는 파일 경로/논문 링크/후보 ID
  - `metric_placeholder`: 반응/구독/댓글 품질 등 추후 관찰값, 아직 성과 주장 금지

## 구현 후보
- 8월 구현 후보: `DecisionLogAdapter`가 Son SNS daily markdown/jsonl에서 5문장 판단 로그를 읽어 `DecisionMoment`와 `RoleJudgmentMap`을 연결한다.
- `GrowthDecisionCard`는 high-end SaaS/bento UI로 요약을 보여주고, 비공개/관리자 모드에서만 소스 refs와 내부 note drawer를 연다.
- 이미지 갤러리/시각 아카이브 탭과도 연결 가능하다: 특정 이미지의 `왜 이 이미지인가` rationale가 어떤 판단 로그/OBD insight에서 왔는지 역참조할 수 있게 한다.

## OBD/Hermes-growth 재사용 메모
- OBD 관점에서 이 모델은 “에이전트가 무엇을 했다”보다 “상황이 어떤 판단 구조로 바뀌었는가”를 남기는 최소 단위가 된다.
- Hermes 성장기에서는 자동화 성과 과장이 아니라 Chris의 책임 있는 판단 기준이 반복적으로 축적되는 증거로 쓰기 좋다.

## 코딩 리스크 / 의존성
- 위험: 실제 업무명, 내부 대화, Telegram/Slack 맥락, 플랫폼 ID가 섞이면 public card로 재사용할 수 없다.
- 의존성: 8월 구현 전 `public_summary`와 `private_context_note`의 필드 경계, private 대시보드 접근 범위, metric 저장 방식 확인 필요.

## Chris에게 요청할 10분 결정 / 관찰
- 오늘은 판단 로그 1개를 5문장으로만 남겨보시면 좋다: `추상화한 상황 → 도움받은 역할 → 줄어든 선택지 → 내가 최종 책임진 기준 → 다음에 볼 신호`.

## 적용된 거버넌스
- 2026-07-22 Muyeol guardrail을 반영했다: 내부 대화/비공개 로그/ID 노출 금지, 성과 수치 과장 금지, 대체 서사 대신 책임 있는 판단 보조 모델로 설명.

## cron 최종 응답으로 보낸 DM 초안
- Chris님께는 7월 계획 전용 원칙과 `PublicSafeDecisionLog` 구조 후보를 짧게 전달한다.

---

## 10:00 Faker direct DM 업데이트 — 시각 아카이브 계획 후보

### 오늘의 웹앱 구조 브리프
- 구조 아이디어: `VisualEvidenceBridge`를 이미지 갤러리 / 시각 아카이브 탭의 보조 데이터로 둔다.
- Go Youn-jung 이미지 카드를 단순한 썸네일 모음으로 두지 않고, 카드 클릭 시 `왜 이 이미지인가` rationale가 어떤 연구 후보·SNS 판단 로그·OBD 키워드에서 왔는지 출처 링크를 보여주는 drawer 구조로 확장한다.
- 제안 화면: `/visuals` masonry grid → image card click → `WhyThisImageDrawer`에서 rationale, metaphor, source agent/date, model, prompt-hidden policy, evidence links를 분리 표시한다.

## Karina sync 업데이트 — 홈 화면은 Go Youn-jung 루프 기반으로 구성

### 홈 화면 레이아웃 방향
- Chris의 최신 방향에 따라 Go Youn-jung의 새 daily visual loop를 단순 `/visuals` archive가 아니라 미래 웹앱 홈 화면의 첫 시각 구조로 사용한다.
- 제안 화면: `/` 상단 `HomeVisualHero` = 최신 Go Youn-jung 3-image set을 1개 lead card + 2개 supporting card로 배치한다.
- 오늘 기준 seed set: `Lantern Guidance`, `Key Possibility`, `Anchor Stability` from `/opt/data/hermes-webapp-build-loop/data/image-gallery/2026-07-22.json`.
- Card grid/home에서는 1080 정적 still만 사용하고, 클릭 시 detail drawer/modal에서 `왜 이 이미지인가`, model, date, source agent, prompt-hidden policy, 그리고 가능한 경우 turntable video를 보여준다.
- `/visuals`는 전체 Pinterest-like archive로 유지하고, 홈은 최신 visual set을 큐레이션한 editorial entry로 둔다.

### 새로 만들거나 업데이트한 후보 산출물
- `/opt/data/hermes-webapp-build-loop/후보s/home-screen-layout.md`
- `/opt/data/hermes-webapp-build-loop/후보s/ia-and-routes.md`
- `/opt/data/hermes-webapp-build-loop/후보s/component-system.md`
- `/opt/data/hermes-webapp-build-loop/후보s/image-gallery-tab.md`
- Gallery metadata now includes `home_component`, `home_placement`, and `home_route` for 2026-07-22 Go Youn-jung items.

### 데이터 구조 후보
- `HomeVisualHero`
  - `latest_visual_set_date`
  - `lead_image_id`
  - `supporting_image_ids`
  - `static_still_paths`
  - `detail_media_paths`
  - `why_this_image_summary`
  - `prompt_visibility_policy`

### 구현 후보
- 8월 후보: `LatestVisualSetAdapter`가 `data/image-gallery/items.jsonl` 또는 날짜별 JSON에서 최신 Go Youn-jung set을 읽고 `/` hero에 매핑한다.
- `HomeVisualHero`는 `/visuals` archive와 같은 `WhyThisImageDrawer`를 공유한다.
- Public home에는 polished visual + short rationale만 보이고, 비공개/관리자에서만 richer source metadata를 노출한다.

### 리스크 / 의존성
- 홈이 이미지 중심이 되더라도 원시 프롬프트, private operational path, Telegram/Slack 맥락은 기본 비노출이다.
- 8월 구현 전 public home과 private 대시보드의 정보 노출 경계를 확정해야 한다.

### 데이터 구조 후보
- `VisualEvidenceBridge`
  - `image_id`: `data/image-gallery/items.jsonl`의 gallery item id
  - `rationale_summary`: 공개 가능한 `왜 이 이미지인가` 1–2문장
  - `metaphor_family`: archive / calibration / decision / accessibility / brand-semantics 등
  - `linked_research_refs`: DOI, arXiv, ACL, DBpia 등 비밀 없는 소스 refs
  - `linked_growth_refs`: 공개 안전 decision log 또는 SNS loop note refs
  - `public_display_level`: public_teaser | private_대시보드 | internal_only
  - `prompt_visibility`: hidden_by_default | 승인된_private_view | 승인된_public_excerpt
  - `governance_note`: 과장/내부 맥락/원본 prompt 노출 방지 메모

### 구현 후보
- 8월 후보: `ImageGalleryAdapter`가 gallery items를 읽고, `VisualEvidenceBridgeAdapter`가 paper 후보s와 growth loop refs를 얇게 연결한다.
- UI 후보: `VisualMasonryCard` + `WhyThisImageDrawer`. 카드에는 이미지/제목/theme/metaphor만, drawer에는 rationale와 evidence links를 보여준다.
- 원시 프롬프트, 비공개 operational path, Telegram/Slack 맥락은 기본 비노출이다.

### OBD/Hermes-growth 재사용 메모
- OBD 관점에서 이미지는 “예쁜 결과물”이 아니라 Chris가 어떤 판단 기준·브랜드 언어·리서치 신호를 시각적으로 재사용할지 보여주는 ontology node가 된다.
- 오늘 연구 후보 중 AI 생성 자동차 디자인의 의미론적 일관성, 접근성 운영 구조, 개인정보 의사결정 논리화는 시각 아카이브가 단순 moodboard를 넘어 “근거가 달린 시각 기억”이 되어야 함을 강화한다.

### 코딩 리스크 / 의존성
- 위험: prompt 전문이나 내부 생성 로그를 drawer에 그대로 넣으면 공개/비공개 경계가 무너진다.
- 의존성: 8월 구현 전 `왜 이 이미지인가`의 공개 가능 수준, source link 공개 범위, private 대시보드 권한을 Chris/Karina가 확정해야 한다.

### Chris에게 요청할 10분 결정 / 관찰
- 오늘은 시각 아카이브 이미지 1장을 고르실 때, “이 이미지를 공개 moodboard로 보여줄지, private 판단 근거로만 둘지”를 한 줄로 표시해주시면 좋다.

### 적용된 거버넌스
- weekly governance folder는 현재 로컬에 없었다. 기존 Muyeol guardrail 방향에 따라 비밀값/원본 prompt/내부 로그 비노출, 연구 링크 보존, 공개 narrative와 비공개 운영 디테일 분리를 적용했다.
