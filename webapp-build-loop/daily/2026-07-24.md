# 2026-07-24 KST — Faker 웹앱 구조 브리프

단계: 2026-07-31까지 7월 계획 전용. 앱 repo 변경 없음, 배포 가능한 코드 없음, build/deploy 없음.

## 사용자-facing DM 초안

Faker입니다.

🧱 오늘의 구조 후보 — `VisualDetailDisclosure`를 홈과 `/visuals`가 공유하는 drawer 계약으로 분리하는 안입니다. 홈의 `HomeVisualHero`는 어제 최종 3종(Lantern Guidance / Moss Planter / Velvet Proof Stamp)만 canonical로 읽고, 오늘 검토 대기 3종(Soft Threshold Gate / Listening Pod / Memory Capsule Safe)은 ‘내일 교체 후보’로만 표시 가능한 preview 상태에 둡니다.

🔗 연결 데이터 — `최종/current-home-visual-set.json`, `items.jsonl`, 그리고 2026-07-24 검토 대기 visual loop입니다. Son/Muyeol 쪽 오늘 렌즈는 `행동-예상 결과-위험-사용자 허락-되돌리기-책임 주체`를 보이게 하는 신뢰 구조입니다.

🧩 OBD/Hermes-growth 의미 — 같은 이미지 카드라도 공개 내러티브와 비공개 운영 디테일을 분리하면, Chris의 시각 은유가 ‘예쁜 갤러리’가 아니라 판단 근거를 안전하게 열어보는 OBD 객체가 됩니다.

⚠️ 8월 구현 전 확인할 것 — drawer에서 source path·model·prompt-hidden 상태는 비공개/관리자으로 둘지, Chris 개인용 화면에서는 일부 metadata를 보여도 되는지 경계 확정이 필요합니다.

✅ 오늘 쌓을 결정 — 이미지 클릭 시 첫 화면은 `왜 이 이미지인가` 2–3줄만 보여주고, source/model/prompt policy는 접힌 metadata 섹션으로 두는 방향이 괜찮을지 봐주세요.

## 구조 브리프

`VisualDetailDisclosure`를 `/`와 `/visuals`가 함께 쓰는 디테일 drawer 계약으로 정의한다:

- 홈 카드 클릭과 시각 아카이브 masonry 카드 클릭은 같은 drawer/modal 패턴을 연다.
- 초기 drawer view는 polish된 제목, 은유, 2–3개의 `why` bullet을 보여준다.
- 정적 still은 카드/기본 미디어로 유지하고, turntable video는 사용 가능할 때 디테일 뷰 안에서만 허용한다.
- 메타데이터는 `public_narrative`, `private_metadata`, `admin_trace` bucket으로 분리한다.
- 검토 대기 visual set은 내부 preview 언어에는 사용할 수 있지만, 최종 promotion 전까지 `HomeVisualHero`가 읽지 않는다.

## 데이터 후보

`VisualDetailDisclosure` 필드:

- `item_id`
- `route_context`: `home | visual_archive | admin_review`
- `title`
- `metaphor`
- `why_public`: 간결한 rationale bullets
- `media`: `still_path`, 선택적 `turntable_video_path`, `video_available`
- `metadata_summary`: date, agent, model family, metaphor family, theme
- `privacy_tier`: `public_narrative | private_personal | admin_trace`
- `prompt_policy`: 정확한 프롬프트는 기본 숨김
- `source_status`: `최종_현재 | archived_최종 | 검토 대기_review | fallback`
- `linked_growth_lens`: action/result/risk/permission/recovery/responsibility 같은 선택적 Son/Muyeol-safe framing

## 8월 구현 후보

Chris/Karina가 repo와 privacy scope를 승인한 뒤, styling-heavy archive 작업보다 drawer 계약을 먼저 구현한다:

1. `HomeVisualSetAdapter`와 `items.jsonl`에서 `VisualDetailDisclosure`로 가는 읽기 전용 normalizer를 만든다.
2. `HomeVisualHero`와 `/visuals`가 함께 쓰는 공유 drawer component 1개를 렌더링한다.
3. privacy tier로 필드를 gate해서 공개 카드가 prompts, private paths, 원시 로그, 토큰, chat identifiers를 절대 누출하지 않게 한다.
4. media fallback 추가: MP4가 없거나 막히면 정적 1080 still로 fallback한다.
5. acceptance check 추가: 화면에 보이는 모든 필드가 최종 또는 승인된 source status로 추적 가능해야 한다.

## OBD/Hermes-growth 재사용 메모

각 시각물을 재사용 가능한 ontology object로 만든다. 카드는 symbolic object, drawer는 rationale layer, 비공개/관리자 메타데이터는 accountable evidence layer가 된다. 신뢰할 수 있는 에이전트가 행동뿐 아니라 결과, 위험, 허락, 복구, 책임 경계를 드러내야 한다는 오늘의 growth lens와도 맞다.

## 리스크 / 의존성

Chris가 개인/비공개 대시보드에서 보는 것과 언젠가 공개될 수 있는 것 사이의 8월 경계가 명시적으로 필요하다. 오늘의 검토 대기 2026-07-24 visual set은 Chris가 최종하거나 finalizer가 승격하기 전까지 홈페이지 canonical이 되면 안 된다.

## Chris 관찰 요청

이미지를 열 때 첫 drawer view는 순수하게 editorial한 `왜 이 이미지인가`로 유지하고, model/source/prompt-policy metadata는 기본 접힘 상태로 두는 것이 좋을까요?
