---
date_kst: 2026-08-01
phase: implementation_ready_waiting_for_scope_lock
agent: faker
---

# 2026-08-01 Faker 웹앱 구조 브리프

## 단계
- 8월 구현 가능 시점에 들어왔지만, Chris/Karina의 repo/workdir, public/private/admin scope, data exposure rule, 우선순위, graph MVP 방식 확인 전까지 실제 앱 저장소 수정은 보류한다.
- 오늘은 구현 시작 전 첫 번째 안전 게이트를 구조 후보로 기록한다.

## 오늘의 웹앱 구조 브리프
- 구조 후보: `AugustScopeLockGate`
- `Harness / Loop / Graph Engineering` 프로젝트의 8월 첫 구현 진입점은 코딩 자체가 아니라, 앱이 읽어도 되는 데이터와 아직 읽으면 안 되는 데이터를 한 장의 scope matrix로 잠그는 게 먼저다.
- 이 게이트는 이후 `HomeVisualSetAdapter`, `VisualSourceStatusGate`, `GraphArtifactCard`가 모두 같은 판단 기준을 쓰게 만든다.

## 데이터 구조 후보
- `ScopeLockDecision` 필드 후보:
  - `repo_workdir`: 확정 전 `unconfirmed`
  - `public_scope`: 공개 홈/공개 카드에 허용되는 narrative field 목록
  - `private_scope`: Chris 개인 dashboard에서 접힘으로 볼 수 있는 field 목록
  - `admin_scope`: 운영 검증과 troubleshooting에만 허용되는 field 목록
  - `allowed_home_statuses`: `final_current`, `final_current_stills_only_no_turntables`, 승인 여부 미정인 partial status
  - `graph_mvp_storage`: `files_first` 또는 `kuzu_or_neo4j_later`
  - `first_week_limit`: read-only data contract + privacy/source gate

## 구현 후보
- 승인 후 첫 30–90분 task는 실제 UI보다 `data/graph/schema.md` 초안과 `HomeVisualSetAdapter` 입력/출력 계약을 고정하는 read-only 작업으로 제한한다.
- 첫 주 허용 범위 후보:
  1. repo/workdir 및 공개 범위 확인
  2. visual data inventory + privacy field matrix
  3. `HomeVisualSetAdapter` read-only normalizer
  4. `final_current_stills_only_no_turntables` fallback gate
  5. Graph MVP는 `Artifact → Theme → Insight/RiskReview` breadcrumb 카드 계약까지만

## OBD/Hermes-growth 재사용 메모
- 이 게이트가 있어야 OBD 웹앱이 “에이전트가 만든 자료를 다 보여주는 화면”이 아니라, 근거·맥락·신뢰를 공개/비공개 경계 안에서 재사용하는 운영 하네스가 된다.

## 코딩 리스크 / 의존성
- Muyeol W31 권고에 따라 8월 첫 주 scope creep 리스크가 가장 크다. repo/scope lock 전에는 route, graph, visual, research, growth, admin을 동시에 구현하지 않는다.
- `pending_review`, exact prompt, remote generation URL, raw 운영 로그, raw delivery/job identifiers는 public UI와 기본 graph data에서 제외한다.

## Chris에게 요청할 10분 결정 또는 관찰
- 오늘 쌓을 결정: 첫 구현 승인을 주실 때 `files-first graph MVP`로 시작할지, 그리고 `final_current_stills_only_no_turntables`를 public home allowed status로 인정할지 두 가지만 먼저 잠그면 좋겠다.

## 적용한 거버넌스
- Muyeol W31 권고 반영: 첫 구현 주는 `repo/scope lock → data inventory/privacy matrix → read-only HomeVisualSetAdapter → still-only fallback gate`까지만 허용한다.
- Graph MVP는 복잡한 network graph가 아니라 `Artifact → Theme → Insight/RiskReview` breadcrumb 카드부터 시작한다.

## 사용자-facing DM 초안
Faker입니다.

1. 🧱 오늘의 구조 후보 — `AugustScopeLockGate`입니다. 8월부터 코딩은 가능하지만, 첫 줄의 코드는 repo/workdir·public/private/admin 범위·데이터 노출 규칙을 잠근 뒤에 들어가는 게 안전합니다.
2. 🔗 연결 데이터 — Muyeol W31 거버넌스와 7/31 `HomeVisualStillOnlyGraphContract`, 최신 `current-home-visual-set.json`의 still-only final 3장입니다.
3. 🧩 OBD/Hermes-growth 의미 — 이 게이트가 있어야 Hermes/OBD 웹앱이 raw 로그 뷰어가 아니라 Harness 위에서 Loop 산출물을 안전하게 Graph Engineering으로 바꾸는 제품 구조가 됩니다.
4. ⚠️ 8월 구현 전 확인할 것 — 첫 주에는 route·graph·visual·research·growth·admin을 동시에 열지 않고, read-only data contract와 privacy/source gate까지만 제한하는 것이 좋습니다.
5. ✅ 오늘 쌓을 결정 — 승인 시 `files-first graph MVP`로 시작할지, 그리고 `final_current_stills_only_no_turntables`를 public home allowed status로 인정할지만 먼저 잠그면 바로 작은 코딩 task로 내려갈 수 있습니다.
