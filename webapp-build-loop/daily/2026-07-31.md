---
date_kst: 2026-07-31
phase: planning_only
agent: faker
---

# 2026-07-31 Faker 웹앱 구조 브리프

## 단계
- 7월 계획 전용: 앱 저장소 수정, 배포 가능한 코드 작성, 빌드/배포 없음.

## 오늘의 웹앱 구조 브리프
- 구조 후보: `HomeVisualStillOnlyGraphContract`
- 최신 canonical 홈 시각 세트가 `final_current_stills_only_no_turntables` 상태로 저장되었으므로, 8월 `HomeVisualHero`와 `/visuals`는 “turntable이 있으면 detail에서 재생, 없으면 still-only detail로 고급스럽게 fallback”하는 계약이 필요하다.
- 그래프 MVP에서는 이 3장을 `Artifact` 노드로 만들되, `media_capability: still_only`, `detail_video_status: unavailable`, `prompt_visibility: hidden_by_default`를 명확히 붙여 public UI와 admin metadata를 분리한다.

## 데이터 구조 후보
- source: `/opt/data/hermes-webapp-build-loop/data/image-gallery/final/current-home-visual-set.json`
- today status: `final_current_stills_only_no_turntables`
- lead: `Soft Evidence Keystone`
- supporting: `Context Field Notebook`, `Trust Envelope Cushion`
- asset mode: 3개 모두 2048 source와 1080 static preview 보유, turntable MP4 없음.

## 구현 후보
- `HomeVisualSetAdapter` 출력에 다음 정규화 필드를 둔다:
  - `completion_state`: `complete_3_stills_only`
  - `media_capability`: `still_only`
  - `detail_video_status`: `unavailable`
  - `detail_media_fallback`: `key_screen_asset_path -> hires_still_path`
- `WhyThisImageDrawer`는 비디오 autoplay를 기본 가정하지 말고, `turntable_video_asset_path`가 없으면 정적 key screen과 rationale만 보여준다.
- `/graph` Artifact card는 network graph보다 `시각 아티팩트 → 테마/은유 → 판단 의미 → 검증/미디어 상태` breadcrumb로 제한한다.

## OBD/Hermes-growth 재사용 메모
- 오늘 세트는 Evidence, Context, Trust 세 축으로 읽힌다. OBD 대시보드에서 “근거가 판단을 받치고, 맥락은 안전하게 보관되며, 가치는 신뢰 가능한 약속으로 전달된다”는 홈 내러티브와 그래프 관계의 첫 예시가 될 수 있다.

## 코딩 리스크 / 의존성
- 8월 구현 전 `final_current_stills_only_no_turntables`를 public home allowed status로 인정할지 확인이 필요하다. 인정한다면 비디오 부재를 public error처럼 보이지 않게 하고, admin/private metadata에서만 상태를 설명해야 한다.

## Chris에게 요청할 10분 결정 또는 관찰
- 오늘은 홈의 첫 Graph MVP 예시를 `Soft Evidence Keystone → Evidence/DecisionStructure Theme`로 시작해도 되는지, 아니면 Research/Growth 카드부터 먼저 그래프화할지 관찰 후보로 둡니다.

## 적용한 거버넌스
- Muyeol W30 권고 반영: 7월 planning-only 유지, read-only adapter 우선, pending source 차단, exact prompt/source/model 운영 메타데이터는 public UI 기본 노출 금지.

## 사용자-facing DM 초안
Faker입니다.

1. 🧱 오늘의 구조 후보 — `HomeVisualStillOnlyGraphContract`입니다. 최신 홈 세트가 3장 모두 final이지만 turntable MP4 없이 still-only이므로, `HomeVisualHero`와 `/visuals`는 “비디오가 있으면 detail에서 재생, 없으면 정적 key screen + 왜 이 이미지인가 drawer”로 자연스럽게 fallback해야 합니다.
2. 🔗 연결 데이터 — `/data/image-gallery/final/current-home-visual-set.json`의 2026-07-31 final set: `Soft Evidence Keystone`, `Context Field Notebook`, `Trust Envelope Cushion`입니다.
3. 🧩 OBD/Hermes-growth 의미 — Evidence / Context / Trust 세 축이 그대로 Graph MVP의 첫 `Artifact → Theme → Insight` 관계가 될 수 있습니다. 홈 비주얼을 장식이 아니라 판단 구조의 입구로 쓰는 방식입니다.
4. ⚠️ 8월 구현 전 확인할 것 — `final_current_stills_only_no_turntables`를 public home allowed status로 인정할지 확인이 필요합니다. 인정한다면 비디오 부재는 public error가 아니라 admin/private metadata로만 접겠습니다.
5. ✅ 오늘 쌓을 결정 — 첫 그래프 예시를 `Soft Evidence Keystone → Evidence/DecisionStructure Theme`로 시작해도 되는지 관찰해 주세요.
