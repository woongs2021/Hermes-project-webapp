---
date_kst: 2026-08-02
phase: implementation_ready_waiting_for_scope_lock
agent: faker
---

# 2026-08-02 Faker 웹앱 구조 브리프

## 단계
- 8월 구현 가능 구간이지만, Chris/Karina의 repo/workdir, public/private/admin scope, data exposure rule, 우선순위, graph MVP 방식 확인 전까지 앱 저장소는 수정하지 않는다.
- 오늘은 최신 Go Youn-jung pending 상태와 2026-08-01 final current 홈 세트를 함께 보고, public 홈으로 승격되지 말아야 할 데이터를 분리하는 구조 후보를 기록한다.

## 오늘의 웹앱 구조 브리프
- 구조 후보: `VisualPromotionQuarantineGate`
- 역할: `pending/2026-08-02.json`처럼 일부 생성·일부 차단된 시각 후보를 public `/`와 기본 `/visuals`에서 격리하고, `final/current-home-visual-set.json`의 최신 승인 세트만 `HomeVisualHero` 기준 소스로 유지한다.
- 사용자-facing 화면은 여전히 2026-08-01 final 3장(`Soft Signal Courtyard`, `Context Cradle Planter`, `Trust Thermos Vessel`)을 읽고, 2026-08-02 partial/pending 후보는 admin/private review queue에서만 다룬다.

## 데이터 구조 후보
- `VisualPromotionQuarantineGate` 필드 후보:
  - `candidate_date_kst`: 후보 생성일
  - `candidate_status`: `pending_review`, `generation_blocked_not_pending_review`, `partial_blocked_by_higgsfield_credits` 등
  - `public_allowed`: 기본 `false`
  - `home_source_action`: `keep_current_final`, `promote_after_explicit_final_save`, `reject_or_wait`
  - `safe_public_preview_fields`: `title`, `theme`, `metaphor`, polish된 `why`
  - `admin_only_fields`: `model`, `source_path`, generation limitation summary, prompt policy
  - `never_public_fields`: exact prompt, remote generation URL, raw operational log, raw delivery/job identifiers
  - `blocker_summary_public_safe`: “일부 생성 리소스가 아직 완성/승격되지 않음” 수준의 짧은 표현

## 구현 후보
- 승인 후 첫 30–90분 task 후보:
  1. `HomeVisualSetAdapter` 출력에 `public_allowed`, `home_source_action`, `quarantine_reason`을 추가한다.
  2. `pending_review`와 `partial_blocked_*`는 public route payload에서 제외되는지 read-only fixture로 검증한다.
  3. final current 세트가 still-only여도 failure로 보지 않고, drawer는 still fallback을 사용한다.
- 아직 실제 app repo/workdir이 확정되지 않았으므로 오늘은 계획 기록만 남긴다.

## OBD/Hermes-growth 재사용 메모
- 이 게이트는 OBD의 핵심인 “판단 전 신호를 안전하게 보류하는 능력”을 웹앱 구조로 옮긴다. 좋은 후보라도 final 승인, 에셋 완성, privacy boundary가 맞기 전에는 public narrative로 승격하지 않는다.
- Graph Engineering에서는 `ArtifactCandidate → HAS_RISK → RiskReview(credit_or_source_status)`와 `Artifact(final_current) → FEEDS → HomeVisualHero`를 분리해, 미완성 후보와 승인 아티팩트가 한 화면에서 혼동되지 않게 한다.

## 코딩 리스크 / 의존성
- Muyeol W31 권고에 따라 remote generation URL, exact prompt, raw job/delivery identifiers는 Chris-facing summary와 public webapp data에 노출하지 않는다.
- 2026-08-02 pending 파일에는 생성 차단/부분 생성 상태가 있으므로, Chris가 final-save를 명시하거나 finalizer가 승격하기 전까지 canonical 홈/공개 그래프 카드로 쓰면 안 된다.

## Chris에게 요청할 10분 결정 또는 관찰
- 오늘 쌓을 결정: `pending_review`/`partial_blocked_*` 시각 후보를 Chris 개인 admin preview에서는 보이게 할지, 아니면 final 승격 전에는 완전히 숨길지 먼저 정하면 `VisualPromotionQuarantineGate` acceptance가 바로 선명해진다.

## 적용한 거버넌스
- Muyeol W31 권고 적용: public UI는 title/metaphor/why 중심, prompt/source/remote URL/raw log는 숨김 또는 admin-only, Graph MVP는 `Artifact → Theme → Insight/RiskReview` breadcrumb 우선.
- 8월 scope risk 방지: repo/scope lock 전 실제 앱 코딩 없음.

## 사용자-facing DM 초안
Faker입니다.

1. 🧱 오늘의 구조 후보 — `VisualPromotionQuarantineGate`입니다. 오늘처럼 일부 생성·일부 차단된 시각 후보는 public 홈에 바로 섞지 않고, 최신 final 홈 세트만 `HomeVisualHero` 기준 소스로 유지하는 게 안전합니다.
2. 🔗 연결 데이터 — `final/current-home-visual-set.json`의 2026-08-01 final 3장과, 아직 final이 아닌 `pending/2026-08-02.json`의 partial 상태를 비교했습니다.
3. 🧩 OBD/Hermes-growth 의미 — OBD는 좋은 신호도 바로 결론으로 밀지 않고, 승인·완성·프라이버시 경계가 맞을 때만 공개 판단으로 승격하는 시스템이어야 합니다.
4. ⚠️ 8월 구현 전 확인할 것 — `pending_review`나 `partial_blocked_*` 데이터를 Chris 개인 admin preview에서는 볼 수 있게 할지, final 전에는 완전히 숨길지 정해야 합니다.
5. ✅ 오늘 쌓을 결정 — 우선 public `/`은 final current만, pending/partial은 admin/private quarantine으로 두는 정책을 후보 결정으로 쌓아두겠습니다.
