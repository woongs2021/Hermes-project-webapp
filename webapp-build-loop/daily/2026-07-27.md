# 2026-07-27 KST — Faker 웹앱 구조 브리프

## 단계
- `planning_only` — 2026-07-31 KST까지 앱 repo 수정, deployable app code 작성, build/deploy 실행 없음.

## 사용자-facing DM 초안
Faker입니다.

🧱 오늘의 구조 후보 — `ResponsibleUXTrace` edge bundle입니다. Graph MVP에서 `ResearchItem → Insight → GrowthQuestion → Artifact/Decision → RiskReview`를 연결하고, 각 edge에 `responsibility_axis`, `privacy_tier`, `validation_status`, `source_access_confidence`, `public_claim`을 붙이는 후보입니다.

🔗 연결 데이터 — 오늘 Son/Muyeol 흐름의 “AI 판단-사용자 제어-수정권-이의제기/중단 경로-책임 주체” 렌즈와, 기존 Graph Engineering 후보(`nodes.jsonl`, `edges.jsonl`, `schema.md`)에서 왔습니다.

🧩 OBD/Hermes-growth 의미 — 설명 가능한 AI를 설명 UI가 아니라 책임 있는 가치 교환 관계로 저장하면, Chris님의 성장 질문·리서치 근거·시각 아티팩트·검증 리스크가 한 장의 판단 그래프로 이어집니다.

⚠️ 8월 구현 전 확인할 것 — `ResponsibleUXTrace`를 공개 `/graph` 카드로 먼저 보여줄지, 비공개 `/growth` 의사결정 카드 안에 먼저 넣을지 범위를 정해야 합니다. `WATCH/HOLD` 근거는 업계 표준처럼 보이면 안 됩니다.

✅ 오늘 쌓을 결정 — 8월 첫 Graph MVP는 force-directed network보다 relation breadcrumb 카드로 시작하는 쪽이 안전해 보입니다. Chris님은 이 관계를 `/graph`의 설명형 카드로 먼저 보고 싶으신지, `/growth`의 판단 로그 카드에서 먼저 보고 싶으신지만 관찰해 주세요.

## 오늘의 웹앱 구조 브리프
- 구조 후보: `ResponsibleUXTrace` edge bundle.
- 핵심 계약: `ResearchItem → Insight → GrowthQuestion → Artifact/Decision → RiskReview` 관계를 파일 기반 그래프 MVP의 edge bundle로 정규화한다.
- 표현 방식: 초기 UI는 복잡한 네트워크 그래프보다 relation breadcrumb 카드.

## 데이터 구조 후보
```json
{
  "edge_bundle": "ResponsibleUXTrace",
  "source_node_type": "ResearchItem",
  "target_flow": ["Insight", "GrowthQuestion", "Artifact_or_Decision", "RiskReview"],
  "edge_fields": [
    "responsibility_axis",
    "privacy_tier",
    "validation_status",
    "source_access_confidence",
    "public_claim",
    "private_note_ref"
  ]
}
```

## 구현 후보
- 8월 승인 후 파일 기반 Graph MVP에서 `edges.jsonl`의 속성 계약으로 먼저 시도한다.
- `nodes.jsonl`/`edges.jsonl`/`schema.md`는 계획 artifact 후보이며, 7월에는 실제 앱 코드나 DB 적재를 만들지 않는다.
- 우선 route 후보: `/graph` 또는 `/growth`.

## OBD/Hermes-growth 재사용 메모
- 오늘 Son/Muyeol의 핵심은 “AI가 설명한다”가 아니라 “사용자가 제어·수정·이의제기/중단·책임 주체를 이해한다”이다.
- 이 구조는 OBD에서 가치 교환, 위임 신뢰, 브랜드 책임, 사용자 agency를 한 관계로 다루는 데 재사용할 수 있다.

## 코딩 리스크 / 의존성
- repo/workdir, public/private/admin scope, graph MVP 파일 경로, `ResponsibleUXTrace`의 첫 노출 route가 미확정이다.
- `WATCH/HOLD` 또는 source-access가 약한 후보는 공개 UI에서 확정 주장으로 보이면 안 된다.
- 원시 Slack/Telegram 로그, private IDs, raw prompts, credential, 정확한 prompt 경로 노출은 금지한다.

## 적용한 거버넌스
- Muyeol W30: 7월 planning-only 유지, 첫 coding week scope creep 방지, read-only adapter/데이터 경계 우선.
- Muyeol 2026-07-27: 연구 신호를 표준/성과로 단정하지 않고, 책임·제어·수정권·사용자 권리 렌즈로 제한.

## Chris에게 요청할 작은 결정/관찰
- Graph MVP의 첫 화면은 `/graph` 설명 카드가 좋을지, `/growth` 의사결정 로그의 relation breadcrumb가 좋을지 관찰 후보로 남긴다.
